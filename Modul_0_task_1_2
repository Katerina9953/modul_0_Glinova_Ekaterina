import numpy as np                                                                          # Импортируем библиотеку.

predict_num = np.random.randint(1,101)                                                      # Объявляем значение переменной.                                                            
number = int(input("Введите число от 1 до 100:"))                                           # Предлагаем пользователю ввести число.
count = 0                                                                                   # Заводим счётчик попыток.
def game_core_v1(number):                                                                   
    while number == predict:                                                                #Условие, при котором цикл завершится.
         count = +1                                                                         # Условие счётчика попыток.
    return (f"Вы совершили {count} попыток, чтобы угадать число")                           # Выход из цикла, если угадали.
    
